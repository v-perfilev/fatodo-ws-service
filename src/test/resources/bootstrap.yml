server:
  address: localhost
  port: 10344

spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
  zipkin:
    enabled: false
  cache:
    type: none

feign:
  client:
    config:
      default:
        connectTimeout: 20000
        readTimeout: 20000

management:
  metrics:
    export:
      influx:
        enabled: false
