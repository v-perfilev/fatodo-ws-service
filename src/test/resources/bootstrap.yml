server:
  address: localhost
  port: 10344

spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
  zipkin:
    enabled: false
  cache:
    type: none

ribbon:
  read-timeout: 60000
  connect-timeout: 60000

management:
  metrics:
    export:
      influx:
        enabled: false
