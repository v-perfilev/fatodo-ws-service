server:
  address: localhost
  port: 10344

spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

management:
  metrics:
    export:
      influx:
        enabled: false
