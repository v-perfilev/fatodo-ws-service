spring:
  application:
    name: base-skeleton

  config:
    import: optional:configserver:https://config-user:${CONFIG_SERVER_PASSWORD:dev}@${CONFIG_SERVER_HOST:localhost}:8888

  cloud:
    config:
      failFast: true
