spring:
  application:
    name: base-skeleton

  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST:localhost}:8888
      fail-fast: true
      username: config-user
      password: ${CONFIG_SERVER_PASSWORD:dev}
